{
  "name": "code-graph",
  "version": "2.0.0",
  "type": "module",
  "description": "Parses a C codebase with tree-sitter and generates a static HTML site of graphviz dataflow diagrams: functions + global variables (read/write edges), call graph, per-file and per-function views. No Obsidian needed — open index.html in a browser.",
  "main": "index.mjs",
  "scripts": {
    "build": "node index.mjs"
  },
  "dependencies": {
    "@hpcc-js/wasm-graphviz": "^1.24.1",
    "tree-sitter": "^0.21.1",
    "tree-sitter-c": "^0.21.4"
  },
  "allowScripts": {
    "tree-sitter@0.21.1": true,
    "tree-sitter-c@0.21.4": true
  }
}
